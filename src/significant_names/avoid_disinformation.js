var hypotenuse;
var accountList = [];

var accountGroup;
var accounts;
var bunchOfAccounts;

function handlingStrings() { }

function storageStrings() { }

var last_selected = 0;
var option_selected = 1;
var first_option = 2;
const new_option = 10;

if (option_selected == last_selected) {
  last_selected = first_option;
} else {
  last_selected = new_option;
}
